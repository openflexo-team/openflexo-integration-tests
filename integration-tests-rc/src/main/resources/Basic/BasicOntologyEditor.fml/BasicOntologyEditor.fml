/**
 * BasicOntologyEditor - a minimal OWL ontology editor.
 *
 * Container view point: it only holds the DiagramSpecification virtual model.
 * Migrated from the deprecated .fml.xml serialization.
 */

@URI("http://www.openflexo.org/openflexo/ViewPoints/Basic/BasicOntologyEditor.fml")
@Version("0.1")
public model BasicOntologyEditor {
}
